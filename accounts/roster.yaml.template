# Account roster — public addresses only. Labels must match .env key prefixes.
# This file is committed to git and visible to the LLM.
# Private keys are in .env (gitignored, never visible to LLM).

accounts:
  main:
    owner_address: "0x..."
    operator_address: "0x..."
    notes: ""
  # farm1:
  #   owner_address: "0x..."
  #   operator_address: "0x..."
  #   notes: ""
