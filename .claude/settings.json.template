{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Bash",
        "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\\.env'; then echo 'BLOCKED: .env access denied' >&2; exit 1; fi"
      }
    ]
  }
}
